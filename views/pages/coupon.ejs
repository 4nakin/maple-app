<% include ../partials/header %>
    <% include ../partials/navigation %>
        <main role="region" class="js-coupon-section container">
            <section role="region" class="list-coupons-section" id="js-list-coupons-section">
              <div class="wrapper">
                <h1>Dashboard</h1>
                <section role="region" class="add-new-coupon-btn">
                  <button type="button" class="btn btn-secondary js-add-new-coupon-btn" data-toggle="modal" data-target="#addNewCouponModal">
                    <img src="images/add-icon@3x.svg" alt="add-icon" class="budicon add-icon">
                    Add Coupon
                  </button>
                </section>
                <% include ../partials/outputMessage %>

                <section role ="region" id="filter">
                  <!--filtering code goes here -->
                </section>
              </div>

              <sectiom role="region" id="coupons" aria-live="polite">
                  <!-- coupons will render here -->
              </section>

            </section>

            <section role="region" id="addNewCouponModelSection" aria-live="assertive">
                <!-- Add modal will render here -->
            </section>

            <section role="region" id="editCouponModelSection" aria-live="assertive">
                <!-- Edit modal will render here -->
            </section>

            <section role="region" id="showuploadedImageModelSection" aria-live="assertive">
                <!-- show uploaded coupon Image modal will render here -->
            </section>

            <section role="region" id="showConfirmDeleteModalSection" aria-live="assertive">
                <!-- show delete confirm modal will render here -->
            </section>

        </main>
      <script src="/javascripts/index.js"></script>
      <script src="javascripts/coupon.js"></script>
<% include ../partials/footer %>
